//Setear un arreglo con los elemento vacios y luego darle valor por medio de un manejador 
// const [array, setArray] = useState([]);

// useEffect(() => {
//     let unmounted = false;

//     if (!unmounted && uploadFiles.imagenes.length > 0) {
//         uploadFiles?.imagenes?.map((item, index) => {
//             return (
//                 setArray(oldArray => [...oldArray, ''])
//             )
//         })
//         // setArray(oldArray => [...oldArray,newValue] );
//     }

//     return () => {
//         unmounted = true;
//     };
// }, [uploadFiles]);

// const textareaChange = (e, index) => {
//     if (e.target.name === "caption") {
//         let temp = array;
//         temp[index] = e.target.value;
//         setArray([...array], temp);
//     }
// };

//RECORRER OBJECTO

// for (var key in files) {
//     var obj = files[key];
//     console.log(obj.size);
//     // ...
//   }

//UNIR OBJECTOS
// const state = Object.assign({ item }, { img });


